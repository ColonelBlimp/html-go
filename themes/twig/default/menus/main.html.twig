    <nav class="h-12 bg-gray-100 border-t-4 border-yellow-500">
      <ul>
{% if content.menus.main is defined %}
{% for node in content.menus.main %}
        <li><a href="{{ content.site.url }}{{ node.key }}">{{ node.name }}</a></li>
{% endfor %}
{% endif %}
      </ul>
    </nav>

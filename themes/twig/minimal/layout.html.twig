<!DOCTYPE html>
<html lang="{{ content.site.language }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ content.title }}{{ content.site.title }}</title>
  </head>
  <body>
    <nav>
    </nav>
    <main>
      <div>
        <h2>{{ content.title }}</h2>
      </div>
{{  }}
    </main>
  </body>
</html>